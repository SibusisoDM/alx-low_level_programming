#include "main.h"

/**
 * _abs - prints the absolute function of the number
 * @n: number to be evaluated
 * Return: absolute value of the number
 */

int _abs(int n)
{
if (n < 0)
return (-n);
else
return (n);
}
